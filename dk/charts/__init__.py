"""Streamlit-free chart rendering (Plotly figures + static PNG export).

Kept separate from dk.ui so headless jobs (the perp scanner's Telegram push)
can render charts without importing Streamlit.
"""
